---
description: Базовый технический стек и правила разработки AI-копилота x-growth-copilot
globs: **/*.*
alwaysApply: true
---

# Tech Stack & AI Rules: x-growth-copilot

## 🛠 Технический Стек
- **Фреймворк:** Next.js (App Router). Использовать Server Components по умолчанию, 'use client' только там, где нужна интерактивность.
- **Стилизация:** Tailwind CSS + shadcn/ui.
- **База Данных:** Supabase (PostgreSQL) + Prisma ORM. *Векторная БД (pgvector) пока отключена по архитектурному решению.*
- **AI Интеграция:** Vercel AI SDK (нативная поддержка Next.js).
- **LLM Модель:** Claude 4.6 Sonnet (лучший движок логики для ведения аналитических диалогов и поиска скрытых смыслов).

## 🧠 Правила работы с AI
1. **Thinking Partner:** Наш приоритет — диалог и глубокий анализ, а не тупая генерация текста. Агенты должны сначала анализировать контент (искать уязвимости, "углы атаки", скрытые смыслы) и обсуждать их со мной, и только в конце упаковывать мысль в мой голос.
2. **Разделение ролей:** Системные промпты для анализа, для ведения чата и для финальной "упаковки" в голос X-поста должны быть физически разделены.
3. **Безопасность:** Никогда не хардкодить `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `SUPABASE_URL`. Всегда использовать переменные окружения `.env.local`.

## 📁 Структура проекта
- `src/app/` - UI и страницы (Dashboard, Deep Dive / Inbox, Voice Bank).
- `src/components/` - UI элементы (shadcn).
- `src/app/api/` - Роуты для вызова AI-агентов (Vercel AI SDK).
- `src/lib/` - Инициализация клиентов (Prisma, Supabase).
- `src/prompts/` - ВАЖНО: Все текстовые системные промпты для AI хранятся здесь в виде констант.
